server:
  port: 8083

spring:
  application:
    name: transaction-service
  datasource:
    url: jdbc:postgresql://localhost:5435/transactions
    username: transactionadmin
    password: transactionadmin
  flyway:
    url: jdbc:postgresql://localhost:5435/transactions
    user: transactionadmin
    password: transactionadmin

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
