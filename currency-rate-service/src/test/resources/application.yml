spring:
  datasource:
    url: jdbc:tc:postgresql:17:///currency
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 50
        order_updates: true
        order_inserts: true